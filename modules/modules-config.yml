# modules/modules-config.yml
# Toggle modules and implementation choices for the repository.

gcode:
  enabled: true
  impl: "basic" # options: basic, robust

presentation:
  enabled: true
  impl: "threejs" # options: threejs, svg

backend:
  enabled: false
  impl: "none"

cli:
  enabled: false
  impl: "none"
