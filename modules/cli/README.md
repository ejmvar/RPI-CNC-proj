# CLI Module (scaffold)

Purpose: command-line utilities for offline processing (e.g., convert G-Code, batch simulate, export compensation meshes).

Starter items:
- `bin/` — small scripts (Python/Node) that operate on files in `Simulator/web/`.

Keep CLI tools dependency-light (prefer Python stdlib or single-file Node scripts) so contributors can run them without a heavy install.
